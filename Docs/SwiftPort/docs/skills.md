# Skills

The Swift port keeps Codex skill discovery, config toggles, plugin skill reads,
and app-server response shapes aligned with Rust where implemented.

For product-level skill behavior, see https://developers.openai.com/codex/skills.
For Swift parity status, see `Docs/PORTING.md`.
