# Slash Commands

Slash-command behavior should match the Rust CLI and TUI once the relevant Swift
runtime slice is ported.

For product-level slash-command behavior, see
https://developers.openai.com/codex/slash-commands. Current Swift TUI/runtime
gaps are tracked in `Docs/PORTING.md`.
