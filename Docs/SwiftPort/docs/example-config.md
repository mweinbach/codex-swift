# Example Config

The Swift port uses the same `config.toml` keys as Rust where implemented.
Examples from the upstream docs should work once the corresponding Swift config
slice is ported.

For canonical examples, see https://developers.openai.com/codex/config-reference.
For Swift status, see `Docs/PORTING.md`.
